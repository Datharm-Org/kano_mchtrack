{
  "icon": "icon-condition-fever",
  "context": {
    "person": true,
    "place": false,
    "expression": "!contact.muted && contact.type === 'person' && user.contact_type === 'chw'"
  },
  "hidden_fields": [
    "report.referral_for_care.patient_id",
    "patient_id"
  ]
}